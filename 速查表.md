# 速查表

忘記怎麼做的時候查這裡。操作流程看 `README.md`，每個零件的完整設定、東西放在哪裡看 `零件手冊.md`。

---

## 1. 常用按鍵

| 按鍵 | 做什麼 |
|---|---|
| **F6** | 執行目前場景（**不要按 F5**） |
| **F8** | 停止執行 |
| **Ctrl+S** | 存檔（分頁名稱旁邊有 `(*)` 就是還沒存） |
| **Ctrl+Z** | 復原 |
| **Delete** | 刪除選取的節點 |
| **Ctrl+D** | 複製選取的節點 |
| **F2** | 改名 |
| 按住**滑鼠中鍵**拖曳 | 平移畫面 |
| **滾輪** | 縮放畫面 |
| **F**（滑鼠在畫面上） | 畫面跳到選取的節點 |

畫地形（選著 `TileMapLayer` 時）：**左鍵畫、右鍵擦**，工具列的矩形工具可以一次拉一整片。
畫完先在場景樹點別的節點，再去動其他東西，不然會不小心畫到磚。

---

## 2. 誰會發出訊號（連線的起點）

選這個零件 → 右邊「**節點**」分頁 → 雙擊訊號 → 選要控制的零件和函式。

| 零件 | 訊號 | 什麼時候發出 |
|---|---|---|
| `Button` 按鈕 | `turned_on` / `turned_off` | 被踩下／放開（依按鈕的 `mode`） |
| `KeyTrigger` 按鍵 | `triggered` | 按下／放開／按住時（依 `trigger`） |
| `EventListener` 事件轉接器 | `triggered` | 選的事件發生時（玩家死亡、進入房間、過關…） |
| `Door` 門 | `opened` / `closed` | 門打開／關上 |
| `Checkpoint` 重生點 | `reached` | 玩家踩到 |
| `Goal` 終點 | `reached` | 玩家踩到 |
| `Pickup` 道具 | `collected` | 被撿走 |
| `Breakable` 可破壞方塊 | `broken` | 被打破 |
| `CrumbleFloor` 崩塌地板 | `crumbled` | 碎掉 |
| `Enemy` 敵人 | `defeated` | 被打倒 |

⚠ 訊號名稱後面有括號的（例如 `launched(body)`、`teleported(body)`、`released(seconds)`）會帶參數，
**不能**連到下面 §3 的函式，連了輸出面板會警告。

---

## 3. 可以被連的函式（連線的終點）

| 零件 | 函式 | 做什麼 |
|---|---|---|
| `Door` 門 | `activate` / `deactivate` / `toggle` | 開／關／切換 |
| `MovingPlatform` 移動平台 | `activate` / `deactivate` / `toggle` | 開始動／停下／切換 |
| `Fan` 風扇 | `activate` / `deactivate` / `toggle` | 開／關／切換 |
| `Player` 玩家 | `kill` | 立刻死亡（之後照常重生） |
| `Player` 玩家 | `force_jump` | 跳一下 |
| `Player` 玩家 | `flip_gravity` | 翻轉重力 |
| `Player` 玩家 | `take_damage` | 扣 1 滴血 |
| `Box`、`Enemy`、`Pickup`、`Breakable`、`CrumbleFloor` | `reset` | 回到一開始的樣子（位置、被撿走、被打破的都復原） |
| `MovingPlatform` 移動平台 | `reset` | 回到起點 |

**Inspector 設定「什麼時候」，訊號決定「做什麼」。**

例子：
- 按鈕開門：`Button` 的 `turned_on` → `Door` 的 `activate`
- 按 E 開關門：`KeyTrigger`（key 選 E）的 `triggered` → `Door` 的 `toggle`
- 按 F 自殺：`KeyTrigger`（key 選 F）的 `triggered` → `Player` 的 `kill`
- 撿到道具時翻轉重力：`Pickup` 的 `collected` → `Player` 的 `flip_gravity`

---

## 4. 每張卡怎麼操作、要搭配什麼

卡片拖到 **Player → Mechanics** 底下。有「按鍵」的卡可以在 Inspector 換鍵，也可以換成滑鼠按鍵。

### 主限制卡

| 卡 | 怎麼操作 | 要搭配 |
|---|---|---|
| 煞車失靈 `NoFriction` | 照常移動，放開方向鍵會一直滑 | — |
| 只能往前 `AutoRun` | 自動一直跑，撞牆轉向，只能控制跳 | — |
| 蓄力青蛙跳 `ChargeJump` | **按住跳躍鍵**蓄力，放開才跳 | — |
| 只能用滑鼠控制 `Slingshot` | 鍵盤不能動；**滑鼠左鍵拖曳**，往反方向彈出去 | — |
| 只用後座力移動 `RecoilMove` | 按方向鍵往**反方向**噴射 | — |
| 彈珠台體質 `PinballBody` | 不會受傷，碰到敵人／尖刺會被彈飛 | `Enemy`、`Spike`、`Lava` |
| 重力翻轉 `GravityFlip` | 按 **Shift** 翻轉（也可以改成落地時／撞牆時自動翻） | 要有天花板 |
| 彈性宇宙 `BouncyWorld` | 撞到牆、地板會反彈 | — |
| 越跑越快 `SpeedRamp` | 一直跑會越來越快、越來越紅 | — |
| 忽大忽小 `SizeShift` | 按 **Shift** 切換大小（也可以改成隨時間自動切） | — |

### 規則卡

| 卡 | 怎麼操作 | 要搭配 |
|---|---|---|
| 黏黏身體 `StickyBody` | 碰到東西就黏住，按跳躍鍵彈開 | — |
| 移動會扣血 `Stamina` | 移動會用掉體力，停下來回復 | — |
| 碰觸即死 `TouchDeath` | 碰到敵人／箱子就死 | `Enemy`、`Box` |
| 存活計時 `SurvivalTimer` | 撐過倒數就過關 | — |
| 停下即死 `StopDeath` | 停下來太久就死 | — |
| 血量流失 `HealthDrain` | 血一直掉，撿金幣補血 | `Pickup`（金幣） |
| 地板是岩漿 `FloorIsLava` | 站在岩漿上會扣血（也可以改成所有地板） | `Lava` |
| 開關世界 `SwitchWorld` | 紅藍方塊定時輪流變實心 | `SwitchBlock`（沒放會警告） |

### 備品卡（`mechanics/_extra/`）

| 卡 | 怎麼操作 | 要搭配 |
|---|---|---|
| 二段跳 `DoubleJump` | 空中再按一次跳躍 | — |
| 衝刺 `Dash` | 按 **Shift** 衝刺 | — |
| 蹬牆跳 `WallJump` | 貼著牆按跳躍 | 牆 |
| 黏黏地板 `StickyFloor` | 踩在地板上變慢或黏住 | — |
| 磁力 `Magnet` | 自動把附近的箱子吸過來 | `Box` |
| 子彈時間 `TimeSlow` | 按 **Q** 時間變慢 | — |
| 只能踩怪 `StompOnly` | 不能跳，只能踩敵人彈起來 | `Enemy` |

### 攻擊能力（拖到 **Player → Abilities**）

| 能力 | 怎麼操作 |
|---|---|
| 近戰 `Ability_Melee` | 按 **F** 攻擊前方 |
| 遠程 `Ability_Ranged` | 按 **G** 發射子彈（可以開 `aim_at_mouse` 朝滑鼠射） |

⚠ **重力翻轉、忽大忽小、衝刺預設都是 Shift**，一起掛的話要換掉其中一個的鍵，輸出面板會提醒。

---

## 5. 關卡裡預先放好的設定

| 節點 | 做什麼 | 詳細 |
|---|---|---|
| `Player` | 「移動參數」：跑速 `move_speed`、跳多高 `jump_force`、重力 `gravity`、地面摩擦 `ground_friction` | 在場景樹**點選 Player** 調，**不要**點場記板圖示進去改 |
| `ValueSettings_Health` | 調血量、要不要顯示血條 | README §5.2 |
| `KeySettings` | 改操作按鍵、加自己的按鍵 | README §5.3 |
| `RespawnHandler` | 死掉後多久重生、回房間還是整關重來 | 刪掉就不會重生 |
| `Room1`（只有 `_Template`） | 房間範圍，鏡頭會對準它 | README §5.1 |
