# 零件手冊

東西放在哪裡、每個零件有哪些設定、能發出什麼訊號、能被連什麼函式。
只想查快捷鍵或卡片怎麼操作 → 看 `速查表.md`。

---

## 1. 東西在哪裡

左下角「檔案系統」裡的資料夾：

| 資料夾 | 裡面是什麼 | 拖到哪裡 |
|---|---|---|
| `mechanics/` | 18 張機制卡 | Player → **Mechanics** |
| `mechanics/_extra/` | 備品卡（二段跳、衝刺、蹬牆跳…） | Player → **Mechanics** |
| `abilities/` | 攻擊能力：`Ability_Melee` 近戰、`Ability_Ranged` 遠程 | Player → **Abilities** |
| `juice/` | 手感組件（之後的週次才會有） | Player → **Juice** |
| `blocks/` | **所有零件**：按鈕、門、敵人、箱子、房間、按鍵設定…（見 §3、§4） | 關卡底下 |
| `levels/` | 範例關卡：`Showroom` 展示間、`Gym` 練習場、`_Template` 關卡範本、`CardDraw` 抽卡 | 打開來看，**不要改** |
| `levels/_starts/` | 每週的起始場景（`W1_ToyBox` 玩具箱） | 另存到 `_my/` 再改 |
| `_my/` | **你的東西**：自己的關卡、自己的圖片（`_my/art/`） | — |

**不用打開、也不要改的**：`player/`、`autoload/`、`art/`，還有每個資料夾裡的 `_scripts/`、`_base/`（程式碼）。
`mechanics/_extra/Mechanic_TestOnly` 是測試用的，不用管它。

---

## 2. 展示間裡看到的東西去哪裡找

`levels/Showroom.tscn` 裡展示的東西全部都在 **`blocks/`**：

| 展示間的標示 | 檔案 |
|---|---|
| 按鈕 | `blocks/Button.tscn` |
| 按鍵觸發器 | `blocks/KeyTrigger.tscn` |
| 傳送門 | `blocks/Portal.tscn`（要放兩座） |
| 重生點 | `blocks/Checkpoint.tscn` |
| 終點 | `blocks/Goal.tscn` |
| 門 | `blocks/Door.tscn` |
| 移動平台 | `blocks/MovingPlatform.tscn` |
| 風扇 | `blocks/Fan.tscn` |
| 崩塌地板 | `blocks/CrumbleFloor.tscn` |
| 單向平台 | `blocks/OneWayPlatform.tscn` |
| 岩漿 | `blocks/Lava.tscn` |
| 可破壞方塊 | `blocks/Breakable.tscn` |
| 開關方塊 | `blocks/SwitchBlock.tscn` |
| 箱子 | `blocks/Box.tscn` |
| 道具 | `blocks/Pickup.tscn` |
| 彈射台 | `blocks/Launcher.tscn` |
| 尖刺 | `blocks/Spike.tscn` |
| 笨敵人 | `blocks/Enemy.tscn` |
| 地板、牆壁 | 不是零件，用 `TileMapLayer` 畫的（見 `速查表.md` §1） |

展示間最後的三個「小劇場」是零件組合起來的：

| 小劇場 | 怎麼做的 |
|---|---|
| 箱子壓住按鈕 → 門開 | `Button` 的 `turned_on` → `Door` 的 `activate`，`turned_off` → `deactivate` |
| 按鈕 → 移動平台 | `Button` 的 `turned_on` → `MovingPlatform` 的 `activate`，`turned_off` → `deactivate` |
| 按 E → 風扇吹起 | `KeyTrigger` 的 `pressed` → `Fan` 的 `activate`，`hold_ended` → `deactivate` |

想看連線：在展示間點那個按鈕，右邊「節點」分頁就看得到它連到哪裡，畫面上也有黃色虛線。

---

## 3. 零件一覽（`blocks/`）

每個零件的**設定**在 Inspector 調；**訊號**是它會通知別人的事（連線起點）；**函式**是別人可以叫它做的事（連線終點）。
滑鼠停在 Inspector 欄位上會有中文說明。

### 機關：會跟別的零件連動

**按鈕 `Button`**
- 設定：`mode` 觸發方式（踩住才開／踩一下切換／踩一下永久開／被攻擊觸發）、`pressed_by` 誰踩得動（玩家與箱子／只有玩家／只有箱子）
- 訊號：`turned_on` 打開時、`turned_off` 關掉時

**門 `Door`**
- 設定：`open_mode` 開門方式（由訊號控制／鑰匙／金幣）、`required_amount` 要幾個、`consume` 開門時要不要用掉、`start_open` 一開始就開著
- 訊號：`opened`、`closed`
- 函式：`activate` 開、`deactivate` 關、`toggle` 切換
- 鑰匙門、金幣門玩家碰到就自己檢查，不用連線

**移動平台 `MovingPlatform`**
- 設定：`direction` 水平／垂直、`distance_tiles` 來回幾格、`speed` 速度、`start_active` 一開始就在動
- 函式：`activate` 開始動、`deactivate` 停、`toggle` 切換、`reset` 回到起點
- 按鈕 + 垂直移動平台 = 電梯

**風扇 `Fan`**
- 設定：`direction` 吹的方向（上下左右）、`force` 風力、`range_tiles` 吹幾格遠、`start_on` 一開始就開著
- 函式：`activate` 開、`deactivate` 關、`toggle` 切換

**開關方塊 `SwitchBlock`**
- 設定：`color` 紅色組／藍色組
- 要搭配「開關世界」規則卡，紅藍兩組會定時輪流變實心

**傳送門 `Portal`**
- 設定：`pair` 另一座傳送門（在 Inspector 點這一欄選另一座，只要設一邊）、`keep_velocity` 保留速度、`allow_boxes` 箱子也能傳送
- 訊號：`teleported(body)`（帶參數，只給會寫程式的人用）

**彈射台 `Launcher`**
- 設定：`mode` 彈簧／彈跳床、`direction` 方向（上左右）、`force` 力道
- 訊號：`launched(body)`（帶參數，只給會寫程式的人用）

### 地形：會影響玩家的地面

**單向平台 `OneWayPlatform`**：從下面可以跳上去、站在上面不會掉。設定：`width_tiles` 寬幾格

**崩塌地板 `CrumbleFloor`**
- 設定：`break_delay` 踩上去幾秒碎、`respawn_time` 幾秒後長回來（0 = 不會長回來）、`triggered_by` 只有玩家／任何物體
- 訊號：`crumbled` 碎掉時
- 函式：`reset` 長回來

**可破壞方塊 `Breakable`**
- 設定：`durability` 打幾下壞、`respawn_time` 幾秒後長回來（0 = 不會）、`break_by_impact` 高速撞擊也能打破、`impact_speed` 要多快才算
- 訊號：`broken` 被打破時
- 函式：`reset` 長回來

**岩漿 `Lava`**：設定：`damage_per_second` 每秒扣幾滴、`instant_kill` 站上去直接死

**尖刺 `Spike`**：設定：`penalty` 扣血／直接死亡、`damage` 扣幾滴

### 物件：會動、會被撿、會被打

**箱子 `Box`**
- 設定：`weight` 輕／重
- 函式：`reset` 回到原位
- 可以被推、被攻擊擊退、壓住按鈕

**道具 `Pickup`**
- 設定：`kind` 金幣／鑰匙／血包／分數、`amount` 加多少
- 訊號：`collected` 被撿走時
- 函式：`reset` 放回來

**笨敵人 `Enemy`**
- 設定：`speed` 巡邏速度、`health` 血量、`damage` 碰到玩家扣幾滴、`turn_at_ledge` 走到懸崖會不會轉身
- 訊號：`defeated` 被打倒時
- 函式：`reset` 復活並回到原位

### 關卡流程

**重生點 `Checkpoint`**：玩家踩到後，死掉就在這裡重生（同一個房間內）。設定：`repeatable` 可以重複觸發。訊號：`reached`

**終點 `Goal`**：玩家踩到就過關。訊號：`reached`

### 玩家本身（`Player`，範本裡已經有）

在場景樹**點選 Player**，Inspector「移動參數」：`move_speed` 跑速、`jump_force` 跳多高、`gravity` 重力、`ground_friction` 地面摩擦。
⚠ 不要點 Player 旁邊的場記板圖示進去改。

可以被連的函式：`kill` 立刻死亡、`force_jump` 跳一下、`flip_gravity` 翻轉重力、`take_damage` 扣 1 滴血

---

## 4. 常用的設定節點

這些不是關卡裡的機關，是**整個關卡的設定**。`_Template` 和 `W1_ToyBox` 已經放好大部分，點它們調 Inspector 就好。

### 按鍵設定 `KeySettings`（範本已放）

關卡裡所有按鍵都在這裡。

**改基本操作**：Inspector「預設操作」

| 欄位 | 預設 |
|---|---|
| `left_key` / `right_key` / `up_key` / `down_key` | A / D / W / S |
| `jump_key` | 空白鍵 |

- 選 `None` = 不改；方向鍵永遠都能用
- 設定存在你的關卡裡，重灌不會不見。**不要**改「專案設定」的 Input Map

**加自己的按鍵**：把 `blocks/KeyTrigger.tscn` 拖到 **KeySettings 底下**，一個 = 一列

| KeyTrigger 的設定 | 做什麼 |
|---|---|
| 節點名稱 | 這一列叫什麼（F2 改名，例如 `Key_OpenDoor`） |
| `key_source` | 按鍵來源：自訂按鍵（預設）／滑鼠左中右鍵／跟基本操作同一顆鍵 |
| `key` | 選哪一顆鍵（`key_source` 選自訂按鍵時） |
| `same_key_as` | 跟哪個基本操作同一顆鍵（`key_source` 選最後一項時）；基本操作照樣會做 |
| `trigger` | 什麼時候觸發：按下時／放開時／按住時（每一幀） |
| `enabled` | 取消勾選 = 暫時關掉 |

然後「節點」分頁 → 雙擊 `triggered` → 選要控制的零件和函式。
**Inspector 設定「什麼時候」，訊號決定「做什麼」。** 要再加一列就 Ctrl+D。

### 數值設定 `ValueSettings`（範本已放一個血量的）

| 欄位 | 做什麼 |
|---|---|
| `kind` | 哪一種數值，例如「血量」「金幣」（全專案唯一要打字的地方，打錯字會提醒） |
| `start_value` / `max_value` | 一開始多少、最多多少（0 = 不限） |
| `show_in_hud` | 打勾：一開場就顯示在左上角；不勾：不顯示 |
| `reset_on_death` | 死掉時要不要退回（累計分數這種就關掉） |

一種數值放一個。金幣、鑰匙要另外設的話，再從 `blocks/` 拖一個 `ValueSettings.tscn`。

### 重生 `RespawnHandler`（範本已放）

| 欄位 | 做什麼 |
|---|---|
| `delay` | 死掉後隔多久重生 |
| `mode` | 回到目前房間／整關重來 |
| `reset_room_objects` | 重生時把房間裡的箱子、敵人、道具等復原 |
| `send_restart_signal` | 每次重生發出「關卡重新開始」事件（給事件轉接器聽） |

刪掉它 = 死了不會重生（遊戲不會壞，輸出面板會提醒）。

### 事件轉接器 `EventListener`

想要「某件事發生時，做某件事」但那件事不是零件發出的，就用它。

| 欄位 | 做什麼 |
|---|---|
| `event` | 聽哪個事件：玩家死亡時／玩家重生時／玩家受傷時／玩家跳躍時／進入房間時／過關時／撿到道具時／敵人被打倒時 |
| `delay` | 事件發生後隔幾秒才觸發 |

訊號：`triggered`。例子：「玩家死亡時」的 `triggered` → 某扇門的 `activate`（死一次就開一條捷徑）。
可以放很多個，各聽各的事件。

---

## 5. 房間 `Room`

關卡比一個畫面大時，把它切成一格一格的房間：

- 玩家走進哪個房間，**鏡頭就直接跳到那個房間**（不會平滑移動）
- 在哪個房間死掉，**就在那個房間重生**，只有那個房間的箱子、敵人、道具會復原，其他房間維持原樣
- 金幣等數值退回「進入這個房間那一刻」

### 設定

| 欄位 | 做什麼 |
|---|---|
| `width_in_tiles` | 寬幾格（**30 格 = 一個畫面寬**） |
| `height_in_tiles` | 高幾格（**17 格 ≈ 一個畫面高**） |

碰撞範圍會依格數自動產生，不用自己拉。

### 怎麼擺

1. **先開格線吸附**，格線步長 16 × 16（每台電腦設一次，見 README §5.1）
2. `_Template` 已經有一個對齊畫面的 **Room1**。自己的關卡沒有的話，從 `blocks/` 拖一個 `Room.tscn`，框的**左上角**對齊畫面左上角
3. 要第二個房間 → 選 Room 按 **Ctrl+D**，拖到第一個框**旁邊**，邊貼齊，不要重疊也不要留縫
4. F6 走過邊界，鏡頭會一格切過去

### 看得到的東西（只在編輯器）

- **淺藍色框**：房間範圍，左上角有房間名稱
- **黃點**：重生位置，預設在房間底部中央往上兩格

### 換重生位置

在 Room 底下加一個 `Marker2D`（選 Room → 加子節點 → 搜尋 Marker2D），把它拖到你要的地方，黃點會跟著移過去。
同一個房間裡如果有踩過的**重生點 `Checkpoint`**，會優先重生在 Checkpoint。

### 房間比一個畫面大

可以，拉大 `width_in_tiles`／`height_in_tiles` 就好，但鏡頭只會對準房間**中心**，超出畫面的部分看不到。
要看到整間，就把 `Camera2D` 的 `zoom` 調小（x 和 y 填一樣的數字，例如 0.5 = 看兩倍大的範圍）。

### 常見問題

| 狀況 | 原因 |
|---|---|
| 鏡頭沒有換房間 | 兩個 Room 之間留了縫，或玩家根本沒走進框裡 |
| 重生後卡在牆裡 | 黃點畫到磚塊上了，拖一個 `Marker2D` 換位置 |
| 重生點之前撿的道具重生後又出現 | 關卡沒有 Room。有放 Checkpoint 的關卡一定要拖 Room |
